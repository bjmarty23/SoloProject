//halp
  // getTypeLocation = (location) => {
  //   console.log('getting location', 'location', location)
  //   //i want to take the type and zip code and compare them to current data in db.
  //   if(location.type === this.state)
  //   then((response) => {
  //     console.log('response', response);
  //   }).catch((error) => {
  //     console.log('error', error)
  //   })
  // }

  {/* <input
        // type="number"
        // name="zipcode"
        // placeholder="Zipcode"
        // onClick={this.getLocation}
      /> */}